<Window x:Class="OneProject.Desktop.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:OneProject.Desktop"
        mc:Ignorable="d" Title="MainWindow" MinHeight="700" MinWidth="1000" xmlns:models="clr-namespace:OneProject.Desktop.ViewModels"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}" TextElement.FontWeight="Regular" TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="Auto" FontFamily="{DynamicResource MaterialDesignFont}"
        d:DataContext="{d:DesignInstance models:MainWindowModel}" Background="{DynamicResource Brush.Background}">
    <StackPanel>

        <TextBox x:Name="ThemeText" Width="200" />

        <Button x:Name="ThemeChangeBtn" Padding="5" BorderThickness="0" Background="{DynamicResource Brush.Color}"
                Foreground="{DynamicResource Brush.Foreground}" Click="OnThemeBtnClick" />
    </StackPanel>
</Window>
